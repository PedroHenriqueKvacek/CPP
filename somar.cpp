#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main(void)
{
    int A, B, C, X;
    cin >> A >> B >> C;
    cout << A + B + C << endl;

    X = A + B + C;

    cout << X << endl;

    return 0;
}